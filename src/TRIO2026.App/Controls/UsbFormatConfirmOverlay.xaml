<UserControl x:Class="TRIO2026.App.Controls.UsbFormatConfirmOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Collapsed">

    <!-- 全螢幕半透明遮罩 + 置中確認卡片 -->
    <Grid>
        <!-- 背景遮罩 -->
        <Border Background="#80000000" />

        <!-- 確認卡片 -->
        <Border MaxWidth="600" MinWidth="480"
                VerticalAlignment="Center" HorizontalAlignment="Center"
                CornerRadius="20"
                Padding="36,32"
                Margin="32">
            <Border.Background>
                <SolidColorBrush Color="{DynamicResource OverlayBackground}" Opacity="0.97" />
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="0" Opacity="0.5" />
            </Border.Effect>

            <StackPanel>
                <!-- 標題 -->
                <TextBlock x:Name="TitleText"
                           Text="⚠️ USB 專碟專用"
                           FontSize="28" FontWeight="SemiBold"
                           Foreground="{DynamicResource ErrorRedBrush}"
                           Margin="0,0,0,24" />

                <!-- 偵測提示 -->
                <TextBlock x:Name="DetectedText"
                           Text="偵測到 USB 隨身碟:"
                           FontSize="20"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           Margin="0,0,0,16" />

                <!-- 裝置資訊區塊 -->
                <Border Background="#354B70"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,24">
                    <TextBlock x:Name="DeviceInfoText"
                               FontSize="18"
                               FontFamily="Consolas"
                               Foreground="#80D8FF"
                               LineHeight="26" />
                </Border>

                <!-- 警告文字 -->
                <TextBlock x:Name="WarningText"
                           Text="為確保資訊安全，即將執行快速格式化。此操作將清除隨身碟上所有資料。"
                           FontSize="18"
                           Foreground="{DynamicResource ErrorRedBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,32" />

                <!-- 按鈕區 -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            x:Name="CancelButton"
                            Content="取消"
                            Click="CancelButton_Click"
                            Height="56"
                            FontSize="20" FontWeight="SemiBold"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd" Background="#546E7A" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#455A64"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Grid.Column="2"
                            x:Name="ConfirmButton"
                            Content="執行格式化"
                            Click="ConfirmButton_Click"
                            Height="56"
                            FontSize="20" FontWeight="SemiBold"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            IsEnabled="False">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd" Background="#D32F2F" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="Bd" Property="Background" Value="#9E9E9E"/>
                                        <Setter Property="Foreground" Value="#EEEEEE"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#B71C1C"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
